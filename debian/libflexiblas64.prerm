#!/bin/sh
set -e

if [ "$1" != "upgrade" ]
then
	update-alternatives --remove libblas64.so.3-x86_64-linux-gnu \
		/usr/lib/x86_64-linux-gnu/libflexiblas64.so.3

	update-alternatives --remove liblapack64.so.3-x86_64-linux-gnu \
		/usr/lib/x86_64-linux-gnu/libflexiblas64.so.3

fi

#DEBHELPER#

exit 0

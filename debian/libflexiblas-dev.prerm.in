#!/bin/sh

set -e

if [ "$1" != "upgrade" ]
then
    update-alternatives --remove libblas.so /usr/lib/@DEB_HOST_MULTIARCH@/flexiblas/update-alternatives/libblas.so
    update-alternatives --remove liblapack.so /usr/lib/@DEB_HOST_MULTIARCH@/flexiblas/update-alternatives/liblapack.so

fi

#DEBHELPER#

exit 0
